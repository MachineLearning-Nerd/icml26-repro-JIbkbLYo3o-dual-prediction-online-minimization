# C3 evaluator record

Candidate verdict: **VERIFIED** for the main-text `2 beta` theorem and the
parametric primal instability family.

The proof certificate, independent cone checker, and rejecting typo control
are generated by the fixed publication command. The former 102 stability
cells are retained only as historical regression evidence.

Limitation: the paper's DFS order is treated as fixed, as stated by Algorithm
2. The result is not asserted for arbitrary optimal dual solutions; it is
asserted for the dual returned by that algorithm.
